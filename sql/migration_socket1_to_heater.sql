-- Миграция: данные с «Розетки 1» начиная с 27.01.2026 19:24 (Киев) включительно
-- переписать как устройство «Обогреватель» (device_id='obogrevatel').
--
-- Важно: колонка называется `timestamp` — в MySQL это зарезервированное слово,
-- в условии ОБЯЗАТЕЛЬНО писать в обратных кавычках: `timestamp`, иначе сравнение ломается.

-- Если timestamp хранится в UTC (типично для Cloud SQL):
-- 27.01.2026 19:24 Киев (UTC+2) = 27.01.2026 17:24 UTC
UPDATE power_status
SET device_id = 'obogrevatel',
    device_name = 'Обогреватель'
WHERE device_id = 'bf3c70a960958bcf11ruml'
  AND `timestamp` >= '2026-01-27 17:24:00';

-- Альтернатива: если в колонке уже киевское время:
--   AND `timestamp` >= '2026-01-27 19:24:00'
-- Или явно по Киеву при хранении в UTC:
--   AND `timestamp` >= CONVERT_TZ('2026-01-27 19:24:00', '+02:00', '+00:00')
