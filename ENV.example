# Пример файла переменных окружения
# Скопируйте этот файл в .env и заполните реальными значениями
# НЕ КОММИТЬТЕ .env в Git!

# Tuya API credentials (обязательно)
TUYA_ACCESS_ID=your_tuya_access_id
TUYA_ACCESS_KEY=your_tuya_access_key

# MySQL connection (обязательно)
MYSQL_HOST=your_mysql_host
MYSQL_PORT=3306
MYSQL_DATABASE=your_database_name
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password

# Tuya API URL (опционально)
TUYA_API_URL=https://openapi.tuyaeu.com

# EcoFlow — уровень заряда экофлошки (опционально)
# Нужны: ECOFLOW_EMAIL, ECOFLOW_SPACE_ID и один из:
#   ECOFLOW_HASHED_PASSWORD — SHA-256 хеш пароля (64 hex-символа), как в приложении EcoFlow
#   ECOFLOW_PASSWORD       — обычный пароль (будет приведён к SHA-256 автоматически)
# ECOFLOW_EMAIL=your@email.com
# ECOFLOW_HASHED_PASSWORD=ваш_sha256_hex_пароля_64_символа
# ECOFLOW_PASSWORD=your_plain_password
# ECOFLOW_SPACE_ID=ваш_space_id
# ECOFLOW_BASE_URL=https://api-e.ecoflow.com

# Device IDs (опционально, если не указано - используются значения по умолчанию)
# DEVICE_IDS={"Розетка 1": "device_id_1", "Розетка 2": "device_id_2"}

# Server port (опционально)
PORT=8080
